/**
 * Třída představující jednotlivé klíče využité napříč aplikací
 */
export default class Keys {
  static readonly ACCESS_TOKEN = 'accessToken';
  static readonly IS_AUTHENTICATED = 'isAuthenticated';
  static readonly USER = 'user';
}
